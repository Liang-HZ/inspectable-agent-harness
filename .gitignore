# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.next
out
tsconfig.tsbuildinfo
*.local
data/agent-sessions/

# Model credentials. `*.local` above already covers .env.local, but .env is the
# filename .env.example tells you to create — and this repository is public.
.env

# Local observability credentials, generated from .env.example on first `obs:up`
docs/observability/.env

# Local Claude Code permission settings (machine-specific, not shared project config)
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# VitePress 教程站构建产物
tutorial/.vitepress/dist
tutorial/.vitepress/cache
